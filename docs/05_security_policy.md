# セキュリティ方針（初期版）

顧客DB、写真、録音音声は認証済みかつ権限を持つ利用者だけが利用できる状態を前提とする。通信は暗号化し、ファイルを公開URLで保存しない。管理者以外のユーザー追加を禁止し、利用停止、バックアップ、入力者・更新者の追跡を可能にする。

AI出力は人間承認なしに登録しない。実装前・リリース前には `ai-governance/checklists/security-checklist.md` を確認する。
